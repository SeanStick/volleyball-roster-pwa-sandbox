{
  "name": "volleyball-roster-pwa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx firebase-tools deploy --only hosting",
    "deploy:sandbox": "vite build && npx firebase-tools deploy --only hosting"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4",
    "firebase": "^11.4.0",
    "lucide-react": "^1.16.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.2.0"
  }
}
